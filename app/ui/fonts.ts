import { Ubuntu, Lusitana } from 'next/font/google';
 
export const ubuntu300 = Ubuntu({ 
    weight: "300",
    subsets: ["latin"]
});

export const ubuntu400 = Ubuntu({ 
    weight: "400",
    subsets: ["latin"]
});

export const ubuntu500 = Ubuntu({ 
    weight: "500",
    subsets: ["latin"]
});

export const ubuntu700 = Ubuntu({ 
    weight: "700",
    subsets: ["latin"]
});

export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
});